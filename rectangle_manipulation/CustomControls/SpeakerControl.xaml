<UserControl
    x:Name="userControl"
    x:Class="rectangle_manipulation.CustomControls.SpeakerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rectangle_manipulation.CustomControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Background="AliceBlue" Height="280" Width="200" Margin="8">
        <StackPanel Orientation="Horizontal" Padding="12,12,12,8">
            <TextBlock Text="{Binding FirstName, ElementName=userControl}" FontSize="20" Foreground="Black"/>
            <TextBlock Text="{Binding LastName, ElementName=userControl}" Padding="8,0,0,0" FontSize="20" Foreground="Black"/>
        </StackPanel>
        <TextBlock Padding="12,0,12,12" FontSize="16" Text="{Binding Company, ElementName=userControl}" Foreground="Black"/>
    </StackPanel>
</UserControl>
